#include <iostream>
#include <string>
//#include <bitset>
#include "reader.h"

int main()
{

    std::string in;
    std::cin >> in;
    reader(in);

    //uint16_t a = 65500;
    //std::cout << std::bitset<16>(a) << std::endl; //bitset syntax

    return 0;
}
